[package]
name = "cuda-confidence-cascade"
version = "0.2.0"
edition = "2021"
description = "Bayesian confidence propagation — uses cuda-equipment"

[dependencies]
# cuda-equipment = { path = "../cuda-equipment" }
